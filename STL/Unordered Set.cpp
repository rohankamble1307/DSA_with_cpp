#include <bits/stdc++.h>
using namespace std;

int main() {

    unordered_set<int> st;

    st.insert(1); // {1}
    st.insert(2); // {1,2}
    st.insert(3); // {1,2,3}

    // unique
    // no sorted order
    // O(1) average
    // worst case O(n) (collision)

}
